# Ex06 - 警车双闪灯效（双通道 PWM）

## 任务说明
使用两个独立 PWM 通道，实现两个 LED 的反相渐变闪烁：
- LED A 亮度从 0 变到 255
- LED B 亮度从 255 变到 0
- 两个亮度曲线反相，过渡柔和

## 硬件连接
- LED A 正极 -> Arduino PWM 引脚 9
- LED B 正极 -> Arduino PWM 引脚 10
- 两个 LED 负极 -> 220Ω 电阻 -> GND

## 说明
程序使用 `analogWrite()` 让两个 LED 反向变化，
一个变亮时另一个变暗，显示平滑交替闪烁效果。
